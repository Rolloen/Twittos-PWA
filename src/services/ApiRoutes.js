
const apiUrl = 'http://localhost:3001';

const routes = {
    verifyTokenUrl : apiUrl + '/verify',
    login: apiUrl + '/login',
    register: apiUrl + '/register',
    suggestion: apiUrl + '/suggestion',
    follow: apiUrl + '/follow'
}

export default routes;